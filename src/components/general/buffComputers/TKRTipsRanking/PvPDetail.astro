---
import {PvPDetail} from './PvPDetail';
import { getCollection, getEntry } from 'astro:content';

import {type GeneralElementType } from '@schemas/generalsSchema'

interface Props {
  name: string;
}

const {name } = Astro.props;

if(name === undefined || name === '') {
  throw new Error('name required')
}

const general : GeneralElementType | null = null

const entry = await getEntry('generals', name)
if(entry){

}

---
